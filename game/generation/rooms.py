from __future__ import annotations

# class Rect:
#     """
#     Represents a rectangle in the game generation.
#
#     Parameters
#     ----------
#     x: int
#         The x coordinate of the rectangle. This uses Python's array indexing.
#     y: int
#         The y coordinate of the rectangle. This uses Python's array indexing.
#     width: int
#         The width of the rectangle.
#     height: int
#         The height of the rectangle.
#     """
#
#     def __init__(self, x: int, y: int, width: int, height: int) -> None:
#         self.top_left: Tuple[int, int] = (x, y)
#         self.top_right: Tuple[int, int] = (x + width, y)
#         self.bottom_left: Tuple[int, int] = (x, y + height)
#         self.bottom_right: Tuple[int, int] = (x + width, y + height)
